{{> head}}
{{> header}}

<h1>Organiser Dashboard</h1>

<div class="card-grid">
  <div class="card">
    <h2>Courses</h2>
    <p class="muted">{{courseCount}} course(s) on the system.</p>
    <a class="btn primary" href="/organiser/courses">Manage Courses</a>
  </div>
  <div class="card">
    <h2>Users</h2>
    <p class="muted">{{userCount}} user(s) registered.</p>
    <a class="btn primary" href="/organiser/users">Manage Users</a>
  </div>
</div>

{{> footer}}
